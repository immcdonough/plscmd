% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_lv_summary}
\alias{plot_lv_summary}
\title{Create Combined LV Summary Figure}
\usage{
plot_lv_summary(
  result,
  lv = 1,
  behav_names = NULL,
  region_names = NULL,
  colors = NULL,
  font_sizes = NULL,
  save_path = NULL,
  width = 12,
  height = 10,
  dpi = 300
)
}
\arguments{
\item{result}{A PLS result object from \code{\link{pls_analysis}}}

\item{lv}{Integer; which latent variable to plot (default: 1)}

\item{behav_names}{Character vector of behavior variable names (optional)}

\item{region_names}{Character vector of region names (optional)}

\item{colors}{Named list of colors (see \code{\link{pls_colors}})}

\item{font_sizes}{Named list with base_size, title_size, axis_title_size, axis_text_size}

\item{save_path}{Character; file path to save the figure (optional)}

\item{width}{Numeric; figure width in inches (default: 12)}

\item{height}{Numeric; figure height in inches (default: 10)}

\item{dpi}{Numeric; resolution for saved figure (default: 300)}
}
\value{
A patchwork object combining the plots, or NULL if LV not significant
}
\description{
Creates a multi-panel figure combining behavior correlations, brain-behavior
scatter plot, and bootstrap ratios for a single latent variable.
}
\examples{
\dontrun{
result <- pls_analysis(...)
fig <- plot_lv_summary(result, lv = 1,
         behav_names = c("RT", "Accuracy"),
         save_path = "LV1_summary.png")
}
}
\seealso{
\code{\link{plot_all_significant_lvs}}, \code{\link{plot_bootstrap_ratios}}
}
