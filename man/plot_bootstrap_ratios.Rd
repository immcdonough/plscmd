% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_bootstrap_ratios}
\alias{plot_bootstrap_ratios}
\title{Plot Bootstrap Ratios for Brain Regions}
\usage{
plot_bootstrap_ratios(
  result,
  lv = 1,
  region_names = NULL,
  threshold = 1.96,
  top_n = 30,
  colors = NULL,
  font_sizes = NULL,
  horizontal = TRUE
)
}
\arguments{
\item{result}{A PLS result object from \code{\link{pls_analysis}}}

\item{lv}{Integer; which latent variable to plot (default: 1)}

\item{region_names}{Character vector of region names (optional)}

\item{threshold}{Numeric; bootstrap ratio threshold for significance (default: 1.96)}

\item{top_n}{Integer; maximum number of regions to show if none significant (default: 30)}

\item{colors}{Named list of colors (see \code{\link{pls_colors}})}

\item{font_sizes}{Named list with base_size, title_size, axis_title_size, axis_text_size}

\item{horizontal}{Logical; if TRUE, bars are horizontal (default: TRUE)}
}
\value{
A ggplot2 object
}
\description{
Creates a bar plot of bootstrap ratios for brain regions, showing only
regions where the confidence interval lower bound exceeds the significance
threshold (i.e., reliably significant).
}
\details{
A region is considered reliably significant only if the lower bound of its
bootstrap ratio confidence interval (|BSR| - SE) exceeds the threshold.
This is more conservative than simply checking if |BSR| > threshold.
}
\examples{
\dontrun{
result <- pls_analysis(...)
p <- plot_bootstrap_ratios(result, lv = 1,
       region_names = paste0("ROI_", 1:100))
print(p)
}
}
\seealso{
\code{\link{plot_lv_summary}}, \code{\link{pls_colors}}
}
